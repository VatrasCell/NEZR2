spring:
  main:
    lazy-initialization: true
  datasource:
    url: jdbc:h2:.\db\nezr_v8;MODE=MySQL
  liquibase:
    enabled: true
    change-log: db/master-changelog.xml

logging:
  level:
    liquibase: info
